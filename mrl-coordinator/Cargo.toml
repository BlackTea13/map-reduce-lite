[package]
name = "mrl-coordinator"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.11.0"
tokio = { version = "1.38.0", features = ["rt", "rt-multi-thread", "macros"] }
common = { path = "../common"}
prost = "0.12.6"

[build-dependencies]
tonic-build = "0.11.0"
