syntax = "proto3";
package coordinator;

service Coordinator {
  // Simply send back the request message.
  rpc Jobs (JobsRequest) returns (JobsResponse);
}

message JobsRequest {}

message JobsResponse {
  uint32 job_count = 1;  
}
